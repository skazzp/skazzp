методы строк
indexOf()
includes()
endsWith()
replace() і replaceAll()
slice()
toString(2) - перевод в двоичное число
toString(10) - в десятичное

методы массивов
join(""		-	объединить элементы с помощью ("")
push		-	добавить в конец
pop		-	удалить с конца
shift		-	добавить в начало
unshift 	- 	удалить с начала
slice(з, по)	-	вырезать/обрезать 
splice(0, 2, "і", 15) 	-	удаление/замена/добавление (индекс, количество, элементы)
const replace = arr.splice	-	можно сохранить удаленное/замененное из массива в переменную

OBJECT
for (let key in { 1: 1, 0: 0 }) {
  console.log(key);
} // выведет ключи (ОТСОРТИРУЮТСЯ ОТ МЕНЬШЕГО К БОЛЬШЕМУ)

// Звичайне оголошення функції
function classicAdd(a, b, c) {
  return a + b + c;
}

// Те саме стрілочною функцією
const arrowAdd = (a, b, c) => {
  return a + b + c;
};

const add = (a, b, c) => a + b + c;

Создание объекта через МАП
array.map((element, index) => {
      return {
        value: element * (index + 1),
        index,
      };
    })

КОНТЕКСТ
"use strict"; - строгий режим
Контекст ФУНКЦІЇ ЗВИЧАЙНОЇ завжди береться В МОМЕНТ ВИКЛИКУ
Контекст ФУНКЦІЇ СТРІЛОЧНОЇ завжди береться В МОМЕНТ СТВОРЕННЯ
Щоб СТРІЛОЧНА ФУНКЦІЯ взяла контекст - створити всередині звичайної функції - візьме контекст ЗВИЧАЙНОЇ функції (викликати треба ЗВИЧАЙНУ).
Функція, що САМОВИКЛИКАЄТЬСЯ - контекст завжди WINDOW.
1. Знайти, яка функція викликається